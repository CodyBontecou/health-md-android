{
  "id": "098d398a",
  "title": "P0: Add multi-format export support on Android",
  "tags": [
    "android",
    "parity",
    "p0",
    "exports",
    "formats"
  ],
  "status": "closed",
  "created_at": "2026-06-05T14:38:20.431Z"
}

Implemented.

## What changed
- Added `ExportSettings.exportFormats: Set<ExportFormat>` while preserving legacy `exportFormat`.
- Added settings migration so saved single-format users become a one-item `exportFormats` set.
- Export and Settings format pickers now toggle multiple formats.
- `ExportRepositoryImpl` writes all selected formats per date.
- Markdown + Obsidian Bases collision handled by writing Bases as `<base>-bases.md` when Markdown is also selected.
- Manual export UI blocks with a clear message when no formats are selected.

## Validation
- `./gradlew testDebugUnitTest` passes.
