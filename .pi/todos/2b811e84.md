{
  "id": "2b811e84",
  "title": "P0: Wire Daily Note Injection into export pipeline",
  "tags": [
    "android",
    "parity",
    "p0",
    "daily-notes",
    "exports"
  ],
  "status": "closed",
  "created_at": "2026-06-05T14:38:20.431Z"
}

Implemented.

## What changed
- `ExportRepositoryImpl` now runs `DailyNoteInjector` as an export side effect after aggregate files are planned/written.
- Daily notes are resolved relative to the selected export root via `DailyNoteInjectionSettings.resolvedPath(date)`, not the aggregate export subfolder.
- Added SAF read/write helpers in `FileExportManager` for relative paths.
- Existing content/frontmatter is read and merged; missing notes honor `createIfMissing`.
- Side-effect write failures cause the date export to fail with the existing file-write error path/history behavior.
- Export Preview includes daily note create/update/skip rows and merged content snippets.

## Validation
- `./gradlew testDebugUnitTest` passes.
