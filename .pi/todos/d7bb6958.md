{
  "id": "d7bb6958",
  "title": "P0: Apply metricSelection to Android exports",
  "tags": [
    "android",
    "parity",
    "p0",
    "exports",
    "metrics"
  ],
  "status": "closed",
  "created_at": "2026-06-05T14:38:20.430Z"
}

Implemented.

## What changed
- Added `MetricSelectionState.toDataTypeSelection()` and `HealthData.filtered(MetricSelectionState)`.
- `ExportOrchestrator` now derives an efficient Health Connect category selection from enabled metrics, fetches only those broad categories, and filters individual metrics before exporters/side effects.
- Disabled metrics are omitted from Markdown/frontmatter, Obsidian Bases, JSON, CSV, Daily Note Injection, and Individual Entry Tracking because all receive filtered `HealthData`.
- Added unit tests in `MetricSelectionExportFilterTest` for disabled steps, disabled sleep stage/granular rows, disabled vitals sample, and partial category selection.

## Validation
- `./gradlew testDebugUnitTest` passes.
