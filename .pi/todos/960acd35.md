{
  "id": "960acd35",
  "title": "P4: Prepare Android parity release rollout",
  "tags": [
    "android",
    "parity",
    "p4",
    "release",
    "play-store"
  ],
  "status": "closed",
  "created_at": "2026-06-05T19:11:40.577Z"
}

## Goal
Turn the completed Android parity phases (P0-P3) into a release-ready rollout package.

## Scope
- Bump the Android app version for the parity release.
- Update Play Console release notes from the initial-release placeholder to parity-focused copy.
- Refresh migration/compatibility docs so they describe the completed P0-P3 implementation and current release metadata.
- Add regression coverage that release metadata and Play Store notes stay aligned.

## Acceptance criteria
- `versionName` reflects the parity release and `versionCode` is monotonically bumped from the prior production code.
- Play Console release notes mention Android/iOS export parity, Obsidian/plugin compatibility, and user migration guidance.
- Export-contract docs reference the completed P0-P3 phases and current release metadata.
- Unit tests pass for release-readiness docs/metadata coverage.

## Implementation summary
- Bumped Android release metadata to `versionCode = 11` and `versionName = "1.3.0"`.
- Replaced the Play Console initial-release placeholder with concise v1.3.0 Android/iOS export parity notes under the 500-character Play limit.
- Added Phase 4 rollout status to the gap matrix and refreshed migration/compatibility docs for completed P0-P3 parity.
- Added `ReleaseReadinessTest` to guard app version metadata, Play release notes, and release-readiness docs.

## Validation
- `./gradlew :app:testDebugUnitTest --tests com.healthmd.exportcontract.ReleaseReadinessTest` passes.
- `./gradlew :app:testDebugUnitTest` passes.
